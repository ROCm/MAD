resources:
  accelerators: mi250:1
  cpus: 32+
  memory: 256+
  image_id: rocm/pytorch-training:v25.5
setup:
  pip install accelerate transformers==4.51.3 datasets loguru tqdm
envs:
  HF_HOME: /mnt/hf_home
  ALLOW_ALL_RANKS_OUTPUT: 0
  NCCL_NET_GDR_LEVEL: LOC

volumes: # {mount_path}: {volume_name}
  /mnt/hf_home: hf-home

run: |
  ### env setup 
  # reinstall torch 2 for gfx90a. v25.5 does not match
  pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.3

  env | grep -i NCCL

  ### clone MAD repository
  git clone https://github.com/MotifTechnologies/MAD.git
  cd MAD
  git checkout add_motif

  ### run!
  accelerate launch --config_file accelerate_config_1gpu.yaml train_motif.py